import { LoadingScreen } from "@/components/ui/LoadingScreen";

/** Shown by Next.js only while a route segment is actually streaming in. */
export default function PublicLoading() {
  return <LoadingScreen />;
}
