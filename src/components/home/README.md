Homepage-specific sections (not reused by other routes). Content/copy for all of these lives in `src/content/home.ts`, not inline, so it can later be swapped for Admin-managed data without touching these components.

Implemented so far (in page order): `Hero` (Step 4), `CompanyStatement`, `OurApproach`, `WhatWeDo`, `FeaturedDevelopment` (+ `StatCounter` used by `Achievements`), `Achievements`, `Trust`, `NextExploration` (Step 5).

Not yet built: nothing further below the fold — the homepage story is complete through Step 5.
